# Configuration for package "CarnaQt"

set( @PROJECT_NAME_CAPS@_MAJOR_VERSION      @MAJOR_VERSION@ )
set( @PROJECT_NAME_CAPS@_MINOR_VERSION      @MINOR_VERSION@ )
set( @PROJECT_NAME_CAPS@_RELEASE_VERSION    @PATCH_VERSION@ )

set( @PROJECT_NAME_CAPS@_INCLUDE  $ENV{MEDITEC_LIBS}/@PROJECT_NAME@/@FULL_VERSION@/include )
set( @PROJECT_NAME_CAPS@_LIBDIR   $ENV{MEDITEC_LIBS}/@PROJECT_NAME@/@FULL_VERSION@/bin )
set( @PROJECT_NAME_CAPS@_LIBLIST
        optimized   @TARGET_NAME@
        debug       @TARGET_NAME@@CMAKE_DEBUG_POSTFIX@
    )

include_directories( ${@PROJECT_NAME_CAPS@_INCLUDE} )
link_directories( ${@PROJECT_NAME_CAPS@_LIBDIR} )

include( $ENV{MEDITEC_LIBS}/Eigen/3.0.5/configure.cmake )

